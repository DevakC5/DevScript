# chart.dev — Plotting / Graph Library for DevLang

say "[chart.dev] Plotting Library Loaded"

def bar(labels, values) ->
    return plot_bar(labels, values)
end

def line(labels, values) ->
    return plot_line(labels, values)
end
