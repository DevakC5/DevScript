# lib_demo.dev — Showcasing new Library Features
# Note: Uncomment the following lines after running `dev install img` and `dev install charts`
# import "img"
# import "charts"

say "=== Library Demo ==="
say "To use vision & chart features, run:"
say "  dev install img"
say "  dev install charts"
say "Then uncomment the imports at the top of this file."
